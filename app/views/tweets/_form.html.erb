<%= form_with(model: tweet, local: true) do |form| %>
  
  <div class="field">
    <%= form.text_field :content, size: 85 %>
  </div>

  <div class="actions ">
    <%= form.submit %>
  </div>
<% end %>
